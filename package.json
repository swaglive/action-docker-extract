{
  "name": "action-docker-extract",
  "version": "1.0.0",
  "description": "Extract file(s) from a Docker Image (GitHub Action)",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.js -o dist",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.1",
    "@actions/io": "^1.1.3"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0"
  },
  "license": "MIT"
}
